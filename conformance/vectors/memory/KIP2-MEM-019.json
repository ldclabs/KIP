{
  "schema_version": "1.0",
  "id": "KIP2-MEM-019",
  "title": "Data contract without a mandatory Brain algorithm",
  "description": "Activate two Brains with different admission/ranking/comparison policies and the\nsame valid Profile. Both may conform if each records its basis and enforces the\nsame invariants. A valid alternative algorithm is not rejected for differing\nweights. An algorithm failing comparability/coverage cannot label its output as\nvalidated improvement. Cognitive utility is never an execution Grant.",
  "level": "MUST",
  "class": "black_box",
  "profiles": [
    "KIP-CognitiveMemory"
  ],
  "fixture": {
    "name": "cognitive-consistency",
    "reset": true
  },
  "spec_refs": [
    "profiles/CognitiveMemoryProfile-2.0.md#2-non-goals"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_scenario",
      "args": {
        "id": "KIP2-MEM-019"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/alternative_policies_allowed",
      "operator": "equals",
      "expected": true
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/unvalidated_improvement_claimed",
      "operator": "equals",
      "expected": false
    }
  ]
}
