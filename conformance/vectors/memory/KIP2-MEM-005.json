{
  "schema_version": "1.0",
  "id": "KIP2-MEM-005",
  "title": "Re-trial, delayed outcome and retained replay",
  "description": "Retain TrialRecord T1 and EvaluationRecord E1; re-trial the same revision as T2.\nA late T1 outcome cannot fill T2 quota. New outcome correction changes only new\naggregates/evaluations. Replaying E1 uses its retained exact rule, parameters,\nbaseline, samples and cutoff, not the mutable the current trial pointer. Missing or\nmismatched replay bytes prevent a recomputability claim. Ordinary current reads\nsuffice while the replay artifact is retained; no historical_reads dependency.",
  "level": "MUST",
  "class": "black_box",
  "profiles": [
    "KIP-CognitiveMemory"
  ],
  "fixture": {
    "name": "cognitive-consistency",
    "reset": true
  },
  "spec_refs": [
    "brain/KIP-2.0-Validated-Learning.md#4-trials-and-evaluations"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_scenario",
      "args": {
        "id": "KIP2-MEM-005"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/old_replay_matches",
      "operator": "equals",
      "expected": true
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/late_samples_in_new_trial",
      "operator": "equals",
      "expected": 0
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/old_trial_unchanged",
      "operator": "equals",
      "expected": true
    }
  ]
}
