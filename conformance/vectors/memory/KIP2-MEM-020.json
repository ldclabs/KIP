{
  "schema_version": "1.0",
  "id": "KIP2-MEM-020",
  "title": "Every written mutable plane of a verdict is guarded",
  "description": "Race a verdict with a concurrent status, current_evaluation or MnemonicState update. A verdict that\nread-modify-writes those planes guards all of them and either commits once or\nrefreshes on conflict. The GradingState view is computed and can never be written,\nso no verdict can overwrite a concurrently changed grade. No partial verdict/status commit.",
  "level": "MUST",
  "class": "black_box",
  "profiles": [
    "KIP-CognitiveMemory"
  ],
  "fixture": {
    "name": "cognitive-consistency",
    "reset": true
  },
  "spec_refs": [
    "brain/KIP-2.0-Validated-Learning.md#4-trials-and-evaluations",
    "profiles/CognitiveMemoryProfile-2.0.md#62-gradingstate-computed"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_scenario",
      "args": {
        "id": "KIP2-MEM-020"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/stale_plane_write_rejected",
      "operator": "equals",
      "expected": true
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/partial_verdict_commit",
      "operator": "equals",
      "expected": false
    }
  ]
}
