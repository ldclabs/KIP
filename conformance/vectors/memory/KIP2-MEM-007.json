{
  "schema_version": "1.0",
  "id": "KIP2-MEM-007",
  "title": "Complete basis, context and exact temporal boundary",
  "description": "Two scoped claims differ between work/travel. Empty request context excludes both;\nwork selects only work plus general claims. At an old interval's until equal to the\nnew interval's from, exactly the new value is valid. Reject empty/reversed finite\nintervals and unanchored timestamps. Change each basis coordinate independently\n(snapshot, Schema, identity, policy, trust, authorization view, context, purpose,\nrisk, valid_at): cache reuse is forbidden unless revalidated. Historical belief\nuses historical policy/trust with current read authorization. Missing historical\ncontrol state fails explicitly; new current trust is never silently substituted.",
  "level": "MUST",
  "class": "black_box",
  "profiles": [
    "KIP-Epistemic"
  ],
  "fixture": {
    "name": "cognitive-consistency",
    "reset": true
  },
  "spec_refs": [
    "SPECIFICATION.md#2112-projectionbasis",
    "SPECIFICATION.md#252-world-intervals"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_scenario",
      "args": {
        "id": "KIP2-MEM-007"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/boundary_values",
      "operator": "equals",
      "expected": [
        "new"
      ]
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/empty_context_values",
      "operator": "equals",
      "expected": []
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/basis_mutations_reused",
      "operator": "equals",
      "expected": 0
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/read_mutations",
      "operator": "equals",
      "expected": 0
    }
  ]
}
