{
  "schema_version": "1.0",
  "id": "KIP2-MIF-010",
  "title": "Evidence expansion pins a result and rechecks access",
  "description": "Recall an item at sequence N, change its current value, then expand the item reference. Expansion returns the retained version/basis from N or explicitly reports unavailable, never silently the newer value. Revoke access or erase the retained evidence and repeat. Opaque item/basis refs never confer access or recreate erased data.",
  "level": "OPTIONAL",
  "class": "black_box",
  "profiles": [
    "KIP-Core",
    "KIP-Runtime"
  ],
  "capabilities": [
    "memory_interface"
  ],
  "fixture": {
    "name": "core-basic",
    "reset": true
  },
  "spec_refs": [
    "KIP-2.0-Memory-Interface.md"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_interface_scenario",
      "args": {
        "id": "KIP2-MIF-010"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/expansion_preserves_basis",
      "operator": "equals",
      "expected": true
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/revoked_access_disclosed",
      "operator": "equals",
      "expected": false
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/erased_expansion_unavailable",
      "operator": "equals",
      "expected": true
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/expansion_writes",
      "operator": "equals",
      "expected": 0
    }
  ]
}
