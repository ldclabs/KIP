{
  "schema_version": "1.0",
  "id": "KIP2-MIF-012",
  "title": "Failed and expired progress never satisfy after",
  "description": "Fail processing after source intake; an after recall reports failure/pending coverage rather than assuming the old fact is current. Probe expired/unknown retained progress and require explicit unavailability. A receipt that was available before later correction/erasure still names that processing horizon but does not bypass current dependencies, authority or retention.",
  "level": "OPTIONAL",
  "class": "black_box",
  "profiles": [
    "KIP-Core",
    "KIP-Runtime"
  ],
  "capabilities": [
    "memory_interface"
  ],
  "fixture": {
    "name": "core-basic",
    "reset": true
  },
  "spec_refs": [
    "KIP-2.0-Memory-Interface.md"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_interface_scenario",
      "args": {
        "id": "KIP2-MIF-012"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/failed_receipt_satisfied",
      "operator": "equals",
      "expected": false
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/unknown_progress_satisfied",
      "operator": "equals",
      "expected": false
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/old_availability_bypasses_current_checks",
      "operator": "equals",
      "expected": false
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/erased_content_recreated",
      "operator": "equals",
      "expected": false
    }
  ]
}
