{
  "schema_version": "1.0",
  "id": "KIP2-MIF-009",
  "title": "Forget acknowledgement is not completed erasure",
  "description": "Request semantic forgetting of an exact source-backed memory. Pause controlled backup/summary erasure after removing raw bytes. The binding returns pending/partial coverage and no successful available erased disposition. Complete all in-scope surfaces and verify completion. A hold returns blocked/error and an unavailable plan must not be fabricated.",
  "level": "OPTIONAL",
  "class": "fault_injection",
  "profiles": [
    "KIP-Core",
    "KIP-Runtime"
  ],
  "capabilities": [
    "memory_interface"
  ],
  "fixture": {
    "name": "core-basic",
    "reset": true
  },
  "spec_refs": [
    "KIP-2.0-Memory-Interface.md"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_interface_scenario",
      "args": {
        "id": "KIP2-MIF-009"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/partial_reported_completed",
      "operator": "equals",
      "expected": false
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/complete_after_all_surfaces",
      "operator": "equals",
      "expected": true
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/hold_blocks_completion",
      "operator": "equals",
      "expected": true
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/in_scope_copies_after_completed",
      "operator": "equals",
      "expected": 0
    }
  ]
}
