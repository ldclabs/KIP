\* BUG variant: EXPECT VERSION validated against the prepare-time snapshot
\* instead of commit-time state (violates §32.3 phase 12 / §35.1).
\* Expected: TLC finds a lost update — LogExpectationsConsistent violated.
SPECIFICATION Spec
CONSTANTS
  Clients = {c1, c2}
  Elements = {e1}
  Vals = {v1, v2}
  Keys = {k1, k2}
  CommitTimeValidation = FALSE
  RetainNoEffect = TRUE
INVARIANTS
  LogExpectationsConsistent
CONSTRAINT StateBound
