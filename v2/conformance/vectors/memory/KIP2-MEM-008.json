{
  "schema_version": "1.0",
  "id": "KIP2-MEM-008",
  "title": "Correct an identity decision",
  "description": "Under identity_repair, merge A into B, write records through supplied A and through\nalready-resolved B, then withdraw the merge decision with a correct identity-version\nguard. Current canonical A resolves independently, old raw history is unchanged,\nand affected ambiguous writes stay needs_review. Stale guards, key/canonical-ID\ncollisions and unauthorized control calls fail. Same local key across two owners\ncannot merge through a Capsule without matching declared portable issuer/scope.",
  "level": "OPTIONAL",
  "class": "black_box",
  "profiles": [
    "KIP-CognitiveMemory"
  ],
  "fixture": {
    "name": "cognitive-consistency",
    "reset": true
  },
  "spec_refs": [
    "KIP-2.0-Cognitive-Consistency.md"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_scenario",
      "args": {
        "id": "KIP2-MEM-008"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/source_resolves_independently",
      "operator": "equals",
      "expected": true
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/lost_attribution_invented",
      "operator": "equals",
      "expected": false
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/raw_history_unchanged",
      "operator": "equals",
      "expected": true
    },
    {
      "source": "state",
      "path": "/unresolved_reviewed",
      "operator": "equals",
      "expected": true
    }
  ],
  "capabilities": [
    "identity_repair"
  ]
}
