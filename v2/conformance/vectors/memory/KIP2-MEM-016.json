{
  "schema_version": "1.0",
  "id": "KIP2-MEM-016",
  "title": "Commitments, assigned work and retention clocks",
  "description": "A semantic assigned_to actor has no worker permission without a matching Principal\nGrant. A due Commitment remains explicit and recallable when mnemonic strength is\nlow. Passing due_at is not fulfillment and neither due_at nor world valid_time is\nretention expiry. A read does not change status or counters.",
  "level": "MUST",
  "class": "black_box",
  "profiles": [
    "KIP-CognitiveMemory"
  ],
  "fixture": {
    "name": "cognitive-consistency",
    "reset": true
  },
  "spec_refs": [
    "KIP-2.0-Cognitive-Consistency.md"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_scenario",
      "args": {
        "id": "KIP2-MEM-016"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/due_commitment_visible",
      "operator": "equals",
      "expected": true
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/ungranted_worker_denied",
      "operator": "equals",
      "expected": true
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/automatic_fulfillment",
      "operator": "equals",
      "expected": false
    }
  ]
}
