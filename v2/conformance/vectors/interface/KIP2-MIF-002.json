{
  "schema_version": "1.0",
  "id": "KIP2-MIF-002",
  "title": "Persisted input is not processed memory",
  "description": "Capture a changed fact as source-77 and durably acknowledge recorded intake, then pause formation. A recall after that receipt at a fresh database/index sequence reaches its deadline. It must disclose pending input and cannot silently return the old fact as a complete current answer. Resume formation and make exact or index-backed recall available, then repeat the barrier.",
  "level": "OPTIONAL",
  "class": "fault_injection",
  "profiles": [
    "KIP-Core",
    "KIP-Runtime"
  ],
  "capabilities": [
    "memory_interface"
  ],
  "fixture": {
    "name": "core-basic",
    "reset": true
  },
  "spec_refs": [
    "KIP-2.0-Memory-Interface.md"
  ],
  "steps": [
    {
      "id": "exercise",
      "kind": "harness",
      "action": "exercise_memory_interface_scenario",
      "args": {
        "id": "KIP2-MIF-002"
      }
    }
  ],
  "assertions": [
    {
      "source": "step",
      "step": "exercise",
      "path": "/before_formation_satisfied",
      "operator": "equals",
      "expected": false
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/before_formation_action_eligible",
      "operator": "equals",
      "expected": false
    },
    {
      "source": "step",
      "step": "exercise",
      "path": "/after_formation_satisfied",
      "operator": "equals",
      "expected": true
    }
  ],
  "postconditions": [
    {
      "source": "state",
      "path": "/intake_count",
      "operator": "equals",
      "expected": 1
    }
  ]
}
